# Quick Notes
A portable notes pad. No Muck SDK — it only reads `%MUCK_SETTINGS_PATH%` JSON.

## Settings
- `fontSize` (10–28)
- `theme` (`dark` | `light`)
- `autosave`

Notes are stored in `notes.txt` next to the script.
